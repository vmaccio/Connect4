public class Player {
    private Token color;

    public Player(Token color){
        this.color = color;
    }

    public void addToken(Board board){//igual y no es turno sino otra cosa, solo aviso
        
        int colum = coordinate.obtainCordinate
        
        board[colum][coordinate.getRow(colum,board)] = this.color
    }
}
